# タスクリスト

- [x] 仕様書 (`spec.md`) の作成
- [x] 設計書 (`plan.md`) の作成
- [x] `ClassManager` に `loadMasteryData` を実装
- [x] `ClassManager` に `getLevel` / `getExpProgress` 計算ロジックを実装
- [x] `ClassManager` に `addExperience` を実装
- [x] `ClassManager` の `getModifiers` を熟練度対応に更新
- [x] `styles.css` に熟練度バーとレベルバッジのスタイル追加
- [x] `ClassManager` の `renderList` を更新して熟練度情報を表示
- [x] `app.js` の `finishWorkout` に EXP 加算処理を追加
- [x] レベルアップ時のトースト通知実装
- [x] Playwright テスト作成 (`tests/class-mastery.spec.js`)
- [x] テスト実行とパス確認
- [x] 最終動作確認
