# タスク一覧

1. 要件の反映
   - `index.html` の `.timer` 内で `.quiz-area` をタイマー表示より上に移動する。
2. スタイル調整
   - `styles.css` の `.quiz-area` に上下反転のスタイルを追加する。
   - 既存の余白を調整し、レイアウトが崩れないことを確認する。
3. 動作確認
   - ブラウザで表示を確認し、クイズが上部で上下反転していることを確認する。
